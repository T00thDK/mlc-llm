import React from 'react'
import ReactDOM from 'react-dom/client'
import DBTResponseGenerator from './components/DBTResponseGenerator'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <DBTResponseGenerator />
  </React.StrictMode>,
)
